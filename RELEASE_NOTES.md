# v0.2.29 · 全屏启动与 Codex 皮肤广场（预览）

- 助手默认全屏启动，标题栏提供退出全屏/还原按钮。
- 新增独立的 Codex 皮肤广场：六套渐变预设、搜索、收藏、背景图片、强调色、深浅色、透明度、模糊、字号和焦点调节，以及保存、另存、删除、自有 JSON 导入导出。
- 基于 Codex Dream Skin 的 MIT 渲染层，通过本机 CDP 应用皮肤、恢复原界面，不修改官方安装文件或 API 配置。
- 提供 DreamSkin 社区广场及 Studio 入口。社区 ZIP/Safe CSS、网页一键换肤、后台持续注入尚未集成；自有 JSON 与 DreamSkin ZIP 不互通。
- 使用前先保存工作、完全退出 Codex，再从助手启动换肤模式并应用。采用独立桌面配置目录，首次可能需要重新登录。正常退出 Codex 后皮肤不保留，下次需重新应用。
- 验证：Go race 测试、前端构建、Chromium 渲染注入/清理及界面交互测试。当前开发环境为 Linux，未完成 Windows/macOS 官方 Codex 实机启动与渲染验证，换肤作为预览功能发布。

## v0.2.28 配置优化

- 新建 Codex 配置默认使用 gpt-5.6-sol，推理深度默认 medium；保留已有自定义配置值。
- 模型路由首次自动获取时显示提示；切换页面保留列表和选择，不重复请求。
- 新增手动刷新模型和分组，刷新期间已有模型仍可选择；切换账号会重新获取。

## 模型路由说明

- 新增“模型路由”页面：选择词元神 Key、实际模型，一键启动并配置 Codex，可连接支持 Chat Completions 的 Claude、Gemini、Grok、DeepSeek 等模型。
- 同时显示 Codex 兼容别名与实际请求模型，不在本地静默回退模型。支持流式/非流式回复、图片输入、函数与补丁工具及工具结果回传。
- 自动备份原配置，停止或正常退出时恢复；异常退出后可恢复，检测到外部修改时保留修改和备份。上游 Key 只在内存中使用，本地代理只监听回环地址并要求独立令牌认证。
- 兼容新版 Codex 的 additional_tools 工具声明，补齐命名空间和自由文本工具转换。

使用：打开“模型路由” → 读取可用模型 → 选择目标模型 → 一键启动。随后重新打开 Codex、新建对话，使用期间保持助手运行。目标模型需要支持所用的对话/工具能力；模型列表表示访问权限，实际可用性以请求结果为准。

暂不支持内置联网搜索、服务端对话续接、远端压缩和自定义 CODEX_HOME。长对话请新建会话。

验证：Go race 测试、真实 Codex CLI 配合本地模拟上游的工具调用闭环、浏览器交互测试及 Windows 构建通过。真实网关各模型能力仍取决于上游服务。

下载：Windows 选择 installer.exe；Mac 选择 Universal DMG（Intel / Apple Silicon 通用），ZIP 为备用。macOS 未签名/公证，首次打开方式见 README。

设计参考 CC Switch（MIT），路由为独立 Go 实现。
# v0.2.34 · 路由模型与更新体验修复

- 默认窗口改为最大化启动，不遮挡任务栏。
- 首次启动自动检查更新；Windows 更新完成后自动重新打开助手，安装包会先关闭旧进程。
- 自动获取模型和分组前检查词元神登录状态，支持保存凭据后的自动登录续期。
- 模型路由支持按供应商查看并多选模型，同一 Key 可配置多个模型；菜单显示名、Codex 请求模型和实际上游模型保持一致。
- 路由 `/v1/models` 返回全部已配置模型，并根据请求中的模型选择对应上游模型。
- 修复 Codex 模型目录缺少 `supported_reasoning_levels` 导致配置解析失败的问题。
